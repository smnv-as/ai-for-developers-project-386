{
  "name": "booking-api",
  "version": "1.0.0",
  "type": "module",
  "description": "TypeSpec API for booking service with 30-min slots",
  "scripts": {
    "compile": "tsp compile .",
    "check": "tsp check"
  },
  "devDependencies": {
    "@typespec/compiler": "^1.12.0",
    "@typespec/http": "^1.12.0",
    "@typespec/openapi3": "^1.12.0",
    "@typespec/rest": "^0.82.0"
  },
  "dependencies": {
    "@typespec/openapi": "^1.12.0"
  }
}
