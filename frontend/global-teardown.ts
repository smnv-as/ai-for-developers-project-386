async function globalTeardown() {
  // Servers are stopped automatically by Playwright
  // This file exists because Playwright requires it if webServer is configured
}

export default globalTeardown;